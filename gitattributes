# Remove any specific tracking for those files
git lfs untrack "content image 1.jpg"
git lfs untrack "style image 3.jpg"

# Add updated .gitattributes
git add .gitattributes
git commit -m "Clean up LFS tracking"
